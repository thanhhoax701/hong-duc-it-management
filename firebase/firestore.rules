rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Bản triển khai thực tế: yêu cầu người dùng đăng nhập.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
